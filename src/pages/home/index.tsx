import Container from "components/Container";
import React from "react";

const HomePage = () => {
  return (
    <Container>
      <h1>Home</h1>
    </Container>
  );
};

export default React.memo(HomePage);
